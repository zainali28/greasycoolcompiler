# greasycoolcompiler

Use the following to run the program:

```bash
make
./compiler inputfile.gc outputfile.gc
```
Make sure the input file is in the same folder as this root directory.
Output file will be created in the same directory as the root.
